apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: load-tester
  namespace: prod
  annotations:
    fluxcd.io/ignore: "false"
spec:
  releaseName: load-tester
  chart:
    git: https://flagger.app
    ref: 0.18.0
    path: charts/loadtester
  values:
    fullnameOverride: load-tester
